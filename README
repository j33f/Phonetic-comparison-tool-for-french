This tool is entended to help to compare to strings ans know if those too strings stands for the same thing.

For example, those two strings «comparaison» (which is the well spelled word) ans «komapézon» (which is the misspelled one) are, phoneticly, equivalent in french.

The main function (phoneticize($str)) create a phoneticized version of $str which is a kind of factorized one.

